spring:
  application:
    name: account-service

  kafka:
    bootstrap-servers: localhost:9092

server:
  port: 8082

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/

